# Merge this table into ~/.codex/config.toml or a trusted project's
# .codex/config.toml. Replace /ABSOLUTE/PATH with the extracted distribution.
[mcp_servers.mindmory]
command = "/ABSOLUTE/PATH/bin/mindmory-mcp-stdio"
required = true
startup_timeout_sec = 20
tool_timeout_sec = 60
